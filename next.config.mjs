/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com', 't3.ftcdn.net', 'benevolent-kangaroo-992.convex.cloud', 'upload.wikimedia.org', 'images.unsplash.com'],
    },
};

export default nextConfig;
